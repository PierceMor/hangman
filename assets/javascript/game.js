var words = [
    "Vader", ]

var word = "";
var wins = 0;
var guesses = [""];
var lettersUsed = [""];

word = words[Math.floor(Math.random())*words.length];

    document.addEventListener ("keyup", function(event) {
        if ( event == "word" ) {
            var word = document.getElementByDiv("word");
            innerHTML = "v"
        }

        for (i in word);
        
        console.log(event.key)
    });